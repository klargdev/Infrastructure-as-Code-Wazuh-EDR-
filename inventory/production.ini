# Example production inventory for Wazuh EDR stack
[wazuh_indexer]
localhost ansible_connection=local ansible_become=true

# For all-in-one assisted install we only need the indexer group
# The assistant will install server and dashboard on the same host
